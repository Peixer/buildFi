# Server port (default: 8080)
PORT=8080

# PostgreSQL connection URL. If unset, in-memory store is used.
# Use with Docker Compose: docker compose up -d then set:
DATABASE_URL=postgres://buildfi:buildfi@localhost:5432/buildfi

# Log level (default: info)
# RUST_LOG=info
