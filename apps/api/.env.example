# Server port (default: 8080)
PORT=8080

# Optional: database URL for future persistence. If unset, in-memory store is used.
# DATABASE_URL=sqlite:buildfi.db

# Log level (default: info)
# RUST_LOG=info
